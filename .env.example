# OpenRouter (server-only — never prefix with NEXT_PUBLIC_)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemini-2.0-flash-001
# Optional branding for OpenRouter rankings
OPENROUTER_SITE_URL=http://localhost:3000
OPENROUTER_APP_NAME=EDCN Video Editor

# Optional: default model hint for the client UI (safe to expose)
# NEXT_PUBLIC_OPENROUTER_MODEL=google/gemini-2.0-flash-001
